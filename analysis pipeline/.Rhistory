library('dplyr')
tdata <- read.csv('testing-mentegy.csv')
tdata
setwd("~/Desktop/github/Social-Media-Hate-Speech-Analysis-and-Monitoring/analysis pipeline")
tdata <- read.csv('testing-mentegy.csv')
tdata
tdata <- read.xlsx('testing-mentegy.xlsx')
tdata <- read.xlsx('testing-mentegy.xlsx')
csv
tdata
tdata <- read.csv('testing-mentegy.csv')
tdata
install.packages('vader')
library('vader')
tdata
getVader(tdata$text, incl_nt = T, neu_set = T)
install.packages('vader')
library('vader')
getVader(tdata$text, incl_nt = T, neu_set = T)
library('getVader')
getVader(tdata$text, incl_nt = T, neu_set = T)
library('vader')
library('dplyr')
getVader(tdata$text, incl_nt = T, neu_set = T)
getVader(tdata$text)
get_vader(tdata$text)
tdata
tdata$test <- get_vader(tdata$text)
tdata
type(get_vader(tdata$text))
typeof(get_vader(tdata$text))
get_vader(tdata$text)
get_vader(tdata$text[1])
tdata$text[1]
get_vader(tdata$text[1])$compound
tdata$text[1]
get_vader(tdata$text[i])
for(i in range(length(tdata)))
get_vader(tdata$text[i])
get_vader(tdata$text[1])
tdata$text[1]
get_vader(tdata$text[2])
get_vader(tdata$text[3])
get_vader(tdata$text[4])
get_vader(tdata$text[5])
get_vader(tdata$text[6])
get_vader(tdata$text[7])
get_vader(tdata$text[i])
get_vader(tdata$text[i])
length(tdata)
length(tdata$text)
for( i in 1:length(tdata$text)) {
get_vader(tdata$text[i])
}
print(get_vader(tdata$text[i]))
for( i in 1:length(tdata$text)) {
print(get_vader(tdata$text[i]))
}
get_vader(tdata$text[10])['compound']
print(get_vader(tdata$text[i])['compound'])
for( i in 1:length(tdata$text)) {
print(get_vader(tdata$text[i])['compound'])
}
for( i in 1:length(tdata$text)) {
score <- get_vader(tdata$text[i])['compound']
print(score)
}
score <- get_vader(tdata$text[i])[3]
get_vader(tdata$text[10])[2]
score <- get_vader(tdata$text[i])[2]
for( i in 1:length(tdata$text)) {
score <- get_vader(tdata$text[i])[2]
print(score)
}
print(typeof(score)
}
for( i in 1:length(tdata$text)) {
score <- get_vader(tdata$text[i])[2]
print(typeof(score))
}
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
print(typeof(score))
}
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
print(typeof(score))
}
print(score)
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
print(score)
}
tdata$test <- print(score)
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
tdata$test <- print(score)
}
tdata$text
tdata$test
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
tdata$test <- print(score)
}
tdata$test
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
tdata$test <- print(score)
}
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
tdata$test <- print(as.integer(score))
}
tdata$test <- print(as.double(score))
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
tdata$test <- print(as.double(score))
}
tdata$test
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
tdata$test[i] <- print(as.double(score))
}
tdata$test
write.ctdata$test
data$test
tdata$test
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else
{
tdata$test[i] <- "Yes"
}
}
tdata$test
tdata$Hate
confusionMatrix(tdata$test,tdata$Hate)
library('caret')
confusionMatrix(tdata$test,tdata$Hate)
tdata
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
library('dplyr')
library('vader')
library('caret')
tdata <- read.csv('testing-mentegy.csv')
tdata
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
tdata$test #prediction data
tdata$Hate #available data
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata <- read.csv('testing-mentegy.csv')
tdata$Hate #available data
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
tdata$Hate #available data
tdata$test #prediction data
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata <- read.csv('testing-mentegy.csv')
tdata$Hate #available data
tdata$test #prediction data
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
tdata$test #prediction data
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata$Hate #available data
tdata$test #prediction data
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata
tdata.test #prediction data
tdata[[test,]] #prediction data
tdata[[test]] #prediction data
tdata[c('test')] #prediction data
tdata[0] #prediction data
tdata[1] #prediction data
tdata[c(0)] #prediction data
tdata[[0]] #prediction data
tdata[-1] #prediction data
tdata[1] #prediction data
tdata[1:] #prediction data
tdata[1:0] #prediction data
tdata$test #prediction data
tdata[c('test')] #prediction data
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
tdata[c('test')] #prediction data
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata[c('test')] #prediction data
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata[c('test')] #prediction data
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata[c('test')] #prediction data
tdata
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata[c('test')] #prediction data
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata[c('test')] #prediction data
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
tdata <- read.csv('testing-mentegy.csv')
for( i in 1:length(tdata$text)) {
score <- as.vector(get_vader(tdata$text[i])[2])
if(as.double(score) > 0)
{ tdata$test[i] <- "No"}
else if(as.double(score) < 0)
{
tdata$test[i] <- "Yes"
}
}
confusionMatrix(as.factor(tdata$test),as.factor(tdata$Hate))
